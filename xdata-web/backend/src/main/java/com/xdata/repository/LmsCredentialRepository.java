package com.xdata.repository;

import com.xdata.model.LmsCredential;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LmsCredentialRepository extends JpaRepository<LmsCredential, Integer> {
}
